/* AetherSDR landing-page enhancements.
   Currently empty — the layered hero is built entirely in CSS via
   position:fixed background layers. This file is kept registered in
   mkdocs.yml so future enhancements (e.g. JS-driven motion, hero image
   preload signal, body class toggling) can be added without re-touching
   the config.
*/

(function () {
  'use strict';
  // intentionally empty
})();
